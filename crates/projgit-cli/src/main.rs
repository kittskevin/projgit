//! `projgit` command-line entry point.
//!
//! Pre-implementation placeholder. See `docs/initial-plan.md` Phase 4 for
//! the planned subcommands (`init`, `clone`, `mount`, `umount`, `ls`, `fetch`).

fn main() {
    println!(
        "projgit {} — pre-implementation skeleton.",
        projgit_core::VERSION
    );
    println!("See docs/initial-plan.md for the design and phased plan.");
}
